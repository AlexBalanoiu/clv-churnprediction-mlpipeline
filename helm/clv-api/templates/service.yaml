apiVersion: v1
kind: Service
metadata:
  name: {{ include "clv-api.fullname" . }}
  labels:
    {{- include "clv-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "clv-api.selectorLabels" . | nindent 4 }}